# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfvars
crash.log

# Lambda build artifacts — built fresh in CI, never commit zips
*.zip
modules/lambda/lambda.zip
modules/mediaconvert/trigger.zip
